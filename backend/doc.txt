pip install -r requirements.txt (cài hết thư viện cần)

python version 3.10.11 (ổn định dùng cho paddleocr)

run : python main_vietocr.py  (bản có tiếng việt)
run : python main.py (bản không tiếng việt)
